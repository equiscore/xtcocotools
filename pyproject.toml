[build-system]
requires = [
    "setuptools>=42",
    "wheel",
    "cython",          # if you're using .pyx
    "numpy>=1.16.0"     # add NumPy as a build-time dependency
]
build-backend = "setuptools.build_meta"

